import styled from 'styled-components'

export const TimeWrapper = styled.div`
  display: flex;
  width: auto;
  flex-direction: column;
  text-align: center;
  margin-bottom: 24px;

  .name {
    font-size: 24px;
  }

  .time {
    font-size: 48px;
  }
`
